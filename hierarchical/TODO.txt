Add the references to articles (Buffa-Garau, our preprint, THB-splines)

- Clean refine
- Plot using hsp_eval
- Change name of adaptivity_solve_laplace. Make it a script, instead of a function.
- Make the classes, hierarchical_mesh and hiearchical_space. Move the functions into the classes.
- Implement the truncated basis.
- Change the way to call hsp_eval (also in geopdes_base).
